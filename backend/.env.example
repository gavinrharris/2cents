# Server
PORT=3000

# PostgreSQL (database name: twocents)
# Use either DATABASE_URL or the individual variables below.
DATABASE_URL=postgresql://user:password@localhost:5432/twocents

# Or set each explicitly:
# PGHOST=localhost
# PGPORT=5432
# PGUSER=user
# PGPASSWORD=password
# PGDATABASE=twocents

# JWT (use a long random string in production)
JWT_SECRET=your-jwt-secret-change-in-production

# CORS (comma-separated origins; add the port your frontend runs on)
CORS_ORIGIN=http://localhost:8080,http://127.0.0.1:8080,http://localhost:8082,http://127.0.0.1:8082
